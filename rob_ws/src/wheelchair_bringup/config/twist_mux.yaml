twist_mux:
  ros__parameters:
    topics:
      keyboard:
        topic   : /cmd_vel_keyboard
        timeout : 0.5
        priority: 100
      eeg:
        topic   : /cmd_vel_eeg
        timeout : 0.5
        priority: 30
      voice:
        topic   : /cmd_vel_voice
        timeout : 0.5
        priority: 50
      nav:
        topic   : /cmd_vel
        timeout : 0.5
        priority: 10
    output_topic: /cmd_vel_raw

